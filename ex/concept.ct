define SYS_WRITE equ 1;
define STDOUT equ 1;

local message db "Hello, world!", 10;
define len equ $ - message;

local write(rdi, rsi, rdx) {
    mov rax = SYS_WRITE;
    syscall;
}

pub _start() {
    call write(STDOUT, message, len);
}