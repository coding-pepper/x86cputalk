data message db "Hello, world!";

fun write_stdout(msg rsi, len rdx) ret none {
    mov rax, 1;
    mov rdi, 1;
    syscall;
};

fun exit(exit_code rdi) ret none {
    mov rax, 60;
    syscall;
};

pub fun _start() ret none {
    write_stdout(message, 13);
    exit(0);
};